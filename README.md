# ProtoMorph
A pluggable, encrypted network tunneling tool written in C++. ProtoMorph allows you to create secure tunnels between two systems, encapsulating traffic within various carrier protocols like ICMP, DNS, QUIC, NTP and HTTP/HTTPS.

